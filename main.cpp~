#include <iostream>
#include <cstring>
#include "Node.h"

using namespace std;

int main() {
  Node* node = new Node();
  char firstName[30];
  strcpy(firstName, "Mike");
  node->setName(firstName);
  cout << node->getName() << endl;
  return 0;
}
