#include "names.h"
#include <cstring>
#include <fstream>

Names::Names() {
  size = 101;
  generateArray(size);
}

void Names::generateArray(int size) {
  char* charArr[size];
  fstream firstNames;
  firstNames.open("firstNames.txt", ios::in);
  
}
